<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">YelpCamp</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link" href="/campgrounds">Campgrounds</a>
                <% if(currentUser && currentUser.role !== 'admin'){ %>
                    <% if(currentUser.role === 'user'){ %>
                        <a class="nav-link" href="/campgrounds/favorites">Favorites</a>
                    <% } else { %>
                      <a class="nav-link" href="/campgrounds/myplaces">My places</a>
                    <% } %>
                  <% } %> 
                <a class="nav-link" href="/campgrounds/new">New Campground</a>
                
            </div>
                <form class="d-flex navbar-nav">
                    <input class="form-control me-2" type="search" placeholder="Search Place" name="searchTerm" aria-label="Search">
                    <button class="btn btn-outline-success" type="submit">Search</button>
                </form>
            <div class="navbar-nav ml-auto">
                <% if(currentUser && currentUser.role === 'admin') { %>
                    <a class="nav-link" href="/admin">Admin</a>
                <% } %> 
                <% if(!currentUser) {%>
                <a class="nav-link" href="/login">Login</a>
                <a class="nav-link" href="/register">Register</a>
                <% } else {%>
                <a class="nav-link" href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>