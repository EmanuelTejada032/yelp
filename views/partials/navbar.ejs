<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/campgrounds">YelpCamp</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link" href="/campgrounds">Campgrounds</a>
                <% if(currentUser && currentUser.role !== 'admin'){ %>
                    <% if(currentUser.role === 'user'){ %>
                        <a class="nav-link" href="/campgrounds/favorites">Favorites</a>
                    <% } else { %>
                      <a class="nav-link" href="/campgrounds/favorites">My places</a>
                    <% } %>
                  <% } %> 
                <% if(currentUser && currentUser.role !== 'user') { %>
                    <a class="nav-link" href="/campgrounds/new">New Campground</a>
                <% } %>
                
                
            </div>
            <div class="navbar-nav ml-auto">

                    <% if(currentUser){%>
                        <a class="nav-link" href="/user/<%= currentUser._id%>"><%=currentUser.username%></a>
                    <% } %>
                    <% if(currentUser && currentUser.photo){ %>
                        <a class="nav-link p-0 mx-2" href="/user/<%= currentUser._id%>"><img class="navbarphoto p-0" src="<%=currentUser.photo.thumbnail%>" alt="No image available"></a>
                    <% } else { %>
                        <a class="nav-link p-0 mx-2" href="#"><img class="navbarphoto" src="https://res.cloudinary.com/emanueltejada/image/upload/v1616169865/photos/defaultprofilephoto_owhou1.png" alt="default picture"></a>
                    <% } %> 

                <% if(currentUser && currentUser.role === 'admin') { %>
                    <a class="nav-link" href="/admin">Admin</a>
                <% } %> 
                <% if(!currentUser) {%>
                <a class="nav-link" href="/login">Login</a>
                <a class="nav-link" href="/register">Register</a>
                <% } else {%>
                <a class="nav-link" href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>